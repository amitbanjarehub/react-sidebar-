import React from 'react';
import { Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h4">Dashboard</Typography>
      {/* Add charts or data visualizations here */}
    </div>
  );
};

export default Dashboard;
